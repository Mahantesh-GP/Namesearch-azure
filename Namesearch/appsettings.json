{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Configuration for connecting to Azure OpenAI or similar services. Replace with actual values.
  "AzureOpenAI": {
    "Endpoint": "https://your-openai-endpoint.azure.com/",
    "ApiKey": "YOUR_API_KEY",
    "DeploymentName": "your-model-deployment"
  }
}